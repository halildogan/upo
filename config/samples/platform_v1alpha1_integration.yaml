apiVersion: platform.upo.io/v1alpha1
kind: Integration
metadata:
  name: sample-webhook
  namespace: tenant-sample
spec:
  tenantRef:
    name: sample
  type: webhook
  endpoint: https://example.com/webhook
  healthCheck:
    path: /healthz
