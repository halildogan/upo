apiVersion: v1
kind: Service
metadata:
  name: upo-webhook-service
  namespace: upo-system
  labels:
    app.kubernetes.io/name: unified-platform-operator
spec:
  selector:
    control-plane: controller-manager
  ports:
  - port: 443
    targetPort: 9443
    protocol: TCP
